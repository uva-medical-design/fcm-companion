# FCM Companion — Environment Setup
#
# 1. Copy this file:  cp .env.example .env.local
# 2. Paste the three keys from Matt's Slack DM into .env.local
# 3. Run:  npm install && npm run dev
#
# NEVER commit .env.local to git (it contains secret keys)

NEXT_PUBLIC_SUPABASE_URL=paste-value-from-slack
NEXT_PUBLIC_SUPABASE_ANON_KEY=paste-value-from-slack
ANTHROPIC_API_KEY=paste-value-from-slack
